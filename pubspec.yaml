name: tower_rush_chronicles_ios
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1 # Версия приложения + номер сборки

environment:
  sdk: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  webview_flutter_wkwebview: ^3.23.1
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/images/